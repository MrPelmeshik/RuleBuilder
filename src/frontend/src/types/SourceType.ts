export type SourceType = {
  "id": number,
  "name": string,
  "type":string,
  "host": string,
  "port": number,
  "database": string,
  "defaultShema"?: string,
  "user": string,
  "password"?: string
}